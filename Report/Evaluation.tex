% ====================================/Users/drs/Projects/DTI/Backend/app/Models/basic/figures/basic_BCE_metrics_table=======================================
% Suggested total word count: 2,500 – 3,500 words
% Figures are not counted in word totals at most institutions —
% use them generously.
% ============================================================

\chapter{Evaluation}
\label{sec: evaluation}

Here we will evaluate the performance of our drug-target interaction (DTI) prediction models across multiple iterations of development.
We will compare our final model against a PyTorch-based deep neural network (DNN) implementation and against reported metrics from the literature, specifically the DeepCDA model evaluated on the BindingDB dataset.
Our evaluation will be structured around three key axes: model progression through iterative improvements, sensitivity to data and decision thresholds, and computational performance across hardware platforms.
Through this comprehensive evaluation, we aim to demonstrate the effectiveness of our modeling choices and identify areas for future improvement.

% ============================================================
\section{Evaluation Methodology}
\label{sec: eval_methodology}
% (~200 words)
% Justify your choices BEFORE presenting results.
Within this section, we will outline the components of our evaluation methodology, including the metrics we will use to assess model performance, the data splits for training and testing, and the rationale behind our choice of a decision threshold for classification.
    \subsection{Metrics}
    \label{sec: eval_metrics}
    To evaluate our models, we will find out the following metrics from the training, validation ,and test set predictions:
    \begin{itemize}
        \item Area Under the Receiver Operating Characteristic Curve (AUROC)
        \item Area Under the Precision-Recall Curve (PR-AUC)
        \item F1 Score
        \item Precision
        \item Recall (Sensitivity)
        \item Accuracy
        \item Confusion Matrix
    \end{itemize}

Accuracy is a fundamental metric that measures the performance by finding out the ratio between correct predictions and all predictions.
This alon would be meaningless because in an imbalanced dataset, a model could achieve high accuracy by simply predicting the majority class.
Precision and recall are crucial in the context of DTI prediction, where false positives (predicting a drug-target interaction that does not exist) can lead to wasted resources in experimental validation, while false negatives (failing to predict a true interaction) can result in missed opportunities for drug discovery.
The F1 score balances precision and recall into a single metric, providing a more holistic view of model performance.
Area Under Curve (AUC) metrics, including AUROC and PR-AUC, are threshold-independent measures that evaluate the model's ability to rank positive instances higher than negative ones, visualising both precision-recall and true positive rate-false positive rate trade-offs across all possible thresholds.
Finally the confusion matrix provides a detailed breakdown of true positives, false positives, true negatives, and false negatives, allowing for a granular analysis of model performance. \citep{geeksforgeeks_metrics_2025}.

    \subsection{Data Splits and Reproducibility}
    \label{sec: eval_splits}
We are using a fixed 70/20/10 split of the bindingDB data for our training, validation, and test sets.
However, this is contrasting a 10-fold cross-validation approach used by DeepCDA and MINDG, which means that the reported metrics from those papers are not directly comparable to our results, but rather serve as approximate reference points.
To address all possible corridors of evaluation we will also adjust the data splits to assess any changes in performance.

    \subsection{Decision Threshold}
    \label{sec: eval_threshold}
Another important aspect of our evaluation is the choice of decision threshold for classifying predictions as positive or negative interactions.
We have opted for pAffinity value \> 7.0 determining binder and pAffinity value \< 5.3 determining non-binder with a grey area of 1.70 pAffinity units as mentioned in~\ref{sec: data_threshold}.
However to improve the robustness of our model, we will experiment with these figures to fully evaluate the model.

% ============================================================
\section{Model Evaluation}
\label{sec:model_eval}
Here we will begin the evaluation of our models, opening with a basic NumPy implementation and iteratively improving it through changes to the loss function, architecture, and training procedure.

    % --------------------------------------------------------

    \subsection{Experimental Setup}
    \label{sec: eval_setup}
Before we venture further it's good to determine our setup; we will run each model on the same hardware (Macbook Air M3, 16GB RAM) to ensure a fair comparison of training times and computational efficiency.
We will train each model with maximum of 1000 epochs, but will implement early stopping based on validation loss to prevent overfitting and reduce unnecessary computation.
    % Briefly describe training procedure
    % (e.g. number of epochs, early stopping criteria, hardware used)


    \subsection{Baseline: Basic NumPy Model}
    \label{sec: eval_basic}
    % (~200 words + figures)
    To establish a baseline for our DTI prediction task, we implemented a simple feedforward neural network using only NumPy.
    This model consists of a single hidden layer with sigmoid activations, which is also present on the output layer, and it is trained using Binary Cross-Entropy (BCE) loss.
    We trained the model with these settings:
  \begin{table}[H]
  \centering
  \caption{Hyperparameter selection in the NumPy baseline.
           Dashes (\textemdash) indicate features absent from the baseline model.}
  \label{tab:hyperparams}
  \begin{tabular}{lc}
  \toprule
  \textbf{Parameter} & \textbf{Basic (NumPy)} \\
  \midrule
  \textit{Architecture} \\
  \quad Hidden size          & 32 \\
  \quad Activation           & Sigmoid \\
  \quad Batch normalisation  & \textemdash \\
  \quad Dropout              & \textemdash \\
  \midrule
  \textit{Training} \\
  \quad Loss function        & BCE \\
  \quad Optimiser            & \textemdash \\
  \quad Learning rate        & 0.5 \\
  \quad Weight decay         & \textemdash \\
  \quad Batch size           & Full batch \\
  \quad Max epochs           & \textemdash \\
  \midrule
  \textit{Regularisation} \\
  \quad Early stopping       & \textemdash \\
  \quad LR scheduler         & \textemdash \\
  \bottomrule
  \end{tabular}
  \end{table}

After training this baseline model, we evaluated its performance against the validation and test sets.
The results of key metrics are summarised in the following graph:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/basic/figures/basic_BCE_metrics_table.png}
    \caption{Baseline model performance across key metrics.}
    \label{fig:basic_metrics_table}
\end{figure}
The data in this table shows strong consistency across splits, which is a sign of minimal overfitting and deals well with unseen data.
A recall of $\sim$0.83 indicates that this model is good at catching positive cases, which is important when predicting a false negative is costly.
A strong recall is important for drug disovery, as missing a true drug-target interaction could mean overlooking a potential binding candidate.
The AUROC of $\sim$0.73 suggests that the model has a reasonable ability to distinguish between binders and non-binders, although there is certainly room for improvement.
The Precision score of $\sim$0.71 indicates that when the model predicts a positive interaction, it is correct ~71\% of the time, which is decent but could lead to some false positives that would require experimental validation.

    \subsubsection{Hyperparameter tuning}
To further evaluate the baseline model, we conducted a hyperparameter tuning experiment by varying the learning rate and hidden layer size.
After doing a sweep of learning rate and hidden size comparing these values together, we retreived these graphs comparing the difference in each parameter:
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/basic/figures/sweep_lr_loss_curve.png}
        \caption*{Learning Rate Sweep}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/basic/figures/sweep_hidden_metrics.png}
        \caption*{Hidden Layer Size Sweep}
    \end{minipage}
    \caption{Hyperparameter tuning results for the baseline model.}
    \label{fig:hyperparam_sweep}
\end{figure}
From the learning rate sweep we learned that 0.1 achieves the same result as 0.5 but with a smoother loss curve, which is a sign of more stable training.
From the hidden layer size sweep we learned that accuracy and F1 score are stable across all hidden sizes and AUC-ROC only improves marginally.
This suggests the basic model has reached a boundary in performance imposed by its single layer-architecture, and that further improvements will likely require architectural changes rather than just tuning hyperparameters.

    \subsection{Iteration 1: Training Procedure advancements}
    \label{sec: eval_iter1}
After seeing how our very basic model performed, we have decided to make progressive changes to the training procedure to improve performance.
        \subsubsection{Minibatch Gradient Descent}
To start with we will add minibatch gradient descent because it can provide more stable and efficient updates compared to full batch training, especially on larger datasets.
After making the change we saw a change to the metrics shown below:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/figures/iter1_metrics_table.png}
    \caption{Iteration 1 model performance across key metrics.}
    \label{fig:iter1_metrics_table}
\end{figure}
The addition of mini-batch gradient descent improved recall substantially at the cost of a small drop in precision, reflecting the noisier gradient updates - a known trade-off that the subsequent iterations will aim to address.
And a different loss curve compared to the baseline, this change is because the model is now updating its weights more frequently, which can lead to faster convergence and a smoother loss curve compared to the baseline's full batch training.
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/basic/figures/basic_loss_curve.png}
        \caption*{Baseline (Full-batch GD)}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/figures/iter1_loss_curve.png}
        \caption*{Iteration 1 (Mini-batch GD)}
    \end{minipage}
    \caption{Training loss curves comparing the baseline to Iteration 1.}
    \label{fig:loss_curve_comparison}
\end{figure}
During the training of the baseline model, we can observe a significant instability spike.
This is a characteristic of full-batch gradient descent with an aggressive learning rate.
Mini-batch gradient descent eliminates this instability by providing more frequent updates, which allows the model to adjust its weights more smoothly and avoid large jumps in the loss landscape.
This results in a more stable training process, as evidenced by the smoother loss curve in Iteration 1 compared to the baseline.

    \subsubsection{He initialisation}
To accompany minibatch gradient descent we will also add He initialisation to break the symmetry and prevent vanishing gradients at the start of training \citep{CITE WHOEVER SAID THIS}.
He initialisation works by setting the initial weights of the network using this equation:
\begin{equation}
    W \sim \mathcal{N}\left(0, \sqrt{\frac{2}{n_{\text{in}}}}\right)
\end{equation}
where $n_{\text{in}}$ is the number of input units in the layer.
This method is particularly effective for layers with ReLU activations, as it helps maintain the variance of the activations throughout the network, which can lead to faster convergence and improved performance.
He initialisation was designed for ReLU activations specifically; applying it alongside sigmoid hidden units is a deliberate intermediate step, as ReLU hidden activations are introduced in the following iteration.
Nonetheless, the variance-preserving property of He initialisation still offers an improvement over naive small weight initialisation ($\times 0.01$) regardless of activation function.
These small improvements can be seen within these metrics and comaprison of loss curves:
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter2_metrics_table.png}
        \caption
        {Iteration 2 model performance across key metrics.}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter2_loss_curve.png}
        \caption{Training loss curves comparing Iteration 1 to Iteration 2.}
    \end{minipage}
    \caption{Iteration 2 improvements.}
    \label{fig:iter2_improvements}
\end{figure}
The improvements from Iteration 1 to Iteration 2 are modest but consistent across all metrics.

    \subsubsection{ReLu activations}
To further improve the training stability and representational capacity of our model, we will replace the sigmoid activations in the hidden layer with ReLU (Rectified Linear Unit) activations.
ReLU is defined as:
\begin{equation}
    \text{ReLU}(x) = \max(0, x)
\end{equation}
ReLU addresses the vanishing gradient problem associated with sigmoid activations, as it allows for a constant gradient for positive inputs, which helps maintain the flow of gradients during backpropagation, especially in deeper networks \citep{CITE WHOEVER SAID THIS}.
This change was impactful and can be seen in the metrics and loss curves:
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter3_metrics_table.png}
        \caption{Iteration 3 model performance across key metrics.}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter3_roc_curve.png}
        \caption{AUC-ROC curves comparing Iteration 2 to Iteration 3.}
    \end{minipage}
    \caption{Iteration 3 improvements.}
    \label{fig:iter3_improvements}
\end{figure}
The switch to ReLU activations in Iteration 3 led to a noticeable improvement in all performance metrics, particularly in AUROC and F1 score, which suggests that the model is now better able to capture complex patterns in the data and distinguish between binders and non-binders more effectively.

\subsubsection{Early Stopping}
Our final iteratuion to the training procedure is to add early stopping based on validation loss, which can help prevent overfitting and reduce training time by halting training once the model's performance on the validation set starts to degrade.
After adding early stopping, we observed the following changes in performance:
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter4_metrics_table.png}
        \caption{Iteration 4 model performance across key metrics.}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Backend/app/Models/Training/Iterations/figures/iter4_loss_curve.png}
        \caption{Training loss curves showing early stopping in Iteration 4.}
    \end{minipage}
    \caption{Iteration 4 improvements with early stopping.}
    \label{fig:iter4_improvements}
\end{figure}
As seen in\ref{fig:iter4_improvements}, early stopping led to premature halting of training, mising out on some of the later epochs where the model could have achieved better performance.
This is a common issue with early stopping, as it relies on the validation loss to determine when to stop training, and if the validation loss is noisy or has a local minimum, it can cause the training to stop too early before the model has fully converged.
For these reasons we will not be using early stopping in our final model.
Instead, we will be using iteration 3 as the basis for future advancements to architecture.

    \subsection{Iteration 2: Architecture Advancements}
    \label{sec: eval_iter2}
    % (~250 words + figures)
    %
    % Change made: Add second hidden layer, increase hidden size,
    %              sigmoid → ReLU activations
    %
    % Content:
    %   - Explain vanishing gradient problem with sigmoid in deep networks
    %   - Explain how ReLU addresses this (sparse activation, constant gradient)
    %   - Discuss representational capacity gained from added depth
    %   - Report metrics table
    %   - Show whether overfitting increases (train vs val gap)
    %   - Compare confusion matrices between It1 and It2
    %
    % Suggested figures:
    %   \figure — Confusion matrix comparison: It1 vs It2
    %   \figure — Updated ROC overlay (baseline + It1 + It2)

    \subsection{Iteration 3: New framework}
    \label{sec: eval_iter3}
    % (~250 words + figures)
    %
    % Change made: Add Dropout + Batch Normalisation + mini-batch SGD
    %
    % Content:
    %   - Explain dropout: randomly zeroing activations prevents co-adaptation
    %   - Explain batch normalisation: stabilises training, implicit regularisation
    %   - Explain mini-batch: better gradient estimates, faster convergence
    %   - Show that train/val gap reduces (overfitting addressed)
    %   - Report metrics table
    %   - Threshold sensitivity plot (precision, recall, F1 vs threshold)
    %     — motivate use of 0.5 or argue for an alternative
    %
    % Suggested figures:
    %   \figure — Threshold sensitivity plot
    %   \figure — Training loss curves showing reduced overfitting vs It2

    % --------------------------------------------------------
    \subsection{Full Model Progression Summary}
    \label{sec: eval_progression}
    % (~200 words + figures)
    %
    % Content:
    %   - Single table: all models side by side across all metrics
    %   - Grouped bar chart: all models × all metrics
    %   - Overlay ROC curve: all iterations on one plot
    %   - Discuss the trend — where did the biggest gains come from?
    %   - Identify which change had the most impact and why
    %
    % Suggested figures:
    %   \figure — Grouped bar chart (all iterations, all metrics)
    %   \figure — ROC curve overlay (all iterations)
    %   \figure — AUC progression line graph (iteration number vs AUROC)

    % --------------------------------------------------------
    \subsection{Hyperparameter Sensitivity Analysis}
    \label{sec: eval_hyperparams}
    % (~200 words + figures)
    %
    % Apply to at least the final iterated model and the PyTorch DNN.
    % Suggested hyperparameters to vary:
    %   - Learning rate
    %   - Hidden layer size / dropout rate
    %   - Batch size (PyTorch DNN only)
    %
    % Content:
    %   - Small grid or line plot for each hyperparameter vs AUROC
    %   - Discuss sensitivity: is the model robust or brittle to these choices?
    %   - Report the final chosen values and justify them
    %
    % Suggested figures:
    %   \figure — Hyperparameter sensitivity plots (line graphs)

% ============================================================
\section{Comparison Against PyTorch DNN}
\label{sec: eval_pytorch}
% (~300 words + figures)
%
% Content:
%   - Introduce the PyTorch DNN as a "modern best practices" reference:
%     dual-branch encoders, BatchNorm, ReLU, Adam, BCE, early stopping
%   - Present side-by-side metrics table: Final iterated model vs DNN
%   - Discuss performance gap (or lack thereof) — what does the DNN do
%     that your iterated model does not, and does it matter?
%   - Compare model complexity: parameter counts, training time
%   - Discuss the tradeoff: interpretability / simplicity vs performance
%
% Suggested figures:
%   \figure — Radar / spider chart: Final model vs PyTorch DNN
%   \figure — Training time vs AUROC scatter (all models)

% ============================================================
\section{Comparison Against Literature Baseline (DeepCDA)}
\label{sec: eval_deepcda}
% (~300 words)
%
% Content:
%   - Briefly describe DeepCDA architecture (cite paper)
%   - Present their reported BindingDB metrics from the MINDG comparison table
%   - EXPLICITLY acknowledge methodological differences:
%       * Their evaluation: 10-fold cross-validation
%       * Your evaluation: fixed train/val/test split
%       * State this means comparison is indicative, not definitive
%   - Compare your best model's metrics against DeepCDA's reported values
%   - Discuss where your model is competitive and where it falls short
%   - Reference MINDG as state-of-the-art upper bound (no direct comparison)
%
% Suggested figures:
%   \figure — Radar chart: Your best model vs DeepCDA vs MINDG (reported values)

% ============================================================
\section{Data Sensitivity Analysis}
\label{sec: eval_data}
% (~300 words + figures)

    \subsubsection{Effect of Training Set Size}
    \label{sec: eval_sample_size}
    % (~150 words + figure)
    %
    % Experiment: Train final model on subsets of training data
    %             (e.g. 10%, 25%, 50%, 75%, 100%) and record test AUROC
    %
    % Content:
    %   - Learning curve plot (training size vs AUROC)
    %   - Discuss: does performance plateau? Would more data help?
    %   - Implications for real-world DTI data collection
    %
    % Suggested figures:
    %   \figure — Learning curve (sample size vs AUROC)

    \subsection{Effect of Decision Threshold}
    \label{sec: data_threshold}
    % (~150 words + figure)
    %
    % Experiment: Vary decision threshold from 0.1 to 0.9 on final model
    %
    % Content:
    %   - Plot precision, recall, and F1 vs threshold
    %   - Identify the threshold that maximises F1
    %   - Discuss domain context: in drug screening, is recall or
    %     precision more important? (missing a drug vs wasting lab resources)
    %   - State whether 0.5 is the optimal choice for this problem
    %
    % Suggested figures:
    %   \figure — Precision / Recall / F1 vs decision threshold

% ============================================================
\section{Computational Performance}
\label{sec: eval_compute}
% (~200 words + table)
%
% Content:
%   - Table: model × {parameter count, training time, inference time}
%   - Compare laptop vs lab computer training times for final model
%   - Discuss whether results are reproducible across hardware
%   - Frame argument: a simpler model that trains in minutes on a laptop
%     vs a complex model requiring GPU hours — relevant for resource-limited
%     research settings
%
% Suggested figures:
%   \figure — Bar chart: training time per model

% ============================================================
\section{Evaluation Summary}
\label{sec: eval_summary}
% (~150 words)
%
% A brief closing paragraph that:
%   - Restates which model performed best and by what margin
%   - Identifies the single most impactful improvement across iterations
%   - Acknowledges limitations of the evaluation (split differences,
%     single dataset, no external validation set)
%   - Leads naturally into the Conclusion section