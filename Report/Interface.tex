\chapter{System Interface and API}
This chapter describes the design and implementation of the system interface and API for the Drug-Target interactive Predictor.
It covers the architecture of the system, the design of the backend API, the frontend design, and the integration between the frontend and backend components.
Additionally, it discusses performance considerations, security measures, limitations, and provides a summary of the interface design.
\section{System Architecture}
The system architecture was designed with consideration of performance and speed, as well as an intuitive user interface.
The architecture consists of a backend that contains data and the predictive model, a frontend interface that allows users to interact with the system and visualise results, and an API to link it all together.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{/Users/drs/Projects/DTI/Report/system_architecture.pdf}
    \caption{System architecture of the Drug--Target Interaction Predictor.}
    \label{fig:architecture}
\end{figure}

\section{Backend API Design}
\label{sec:backend_api}
To link the frontend and backend components, a RESTful API was designed to ensure seamless communication between the two.
The API was designed with a focus on simplicity, scalability, and maintainability, allowing for easy integration with the frontend and potential future extensions.
\subsection{Framework and Architecture}
We used FastAPI as the framework for our backend API due to its high performance, ease of use, and robustness \citep{fastapi}.
The API follows a modular architecture, with separate modules for data loading, model inference, virtual screening, and input validation.
This modular design allows for better organisation of code and easier maintenance.
\subsection{Endpoint Specification}
% Group endpoints by domain: prediction, screening, autocomplete, structure, history
The API exposes twelve endpoints, grouped by domain in Table~\ref{tab:endpoints}.
The Prediction and screening endpoints handle model inference, and the autocomplete and filtering endpoints support the search and drug selection workflows.
The history endpoints allow the user review and clear previous predictions.
The structure endpoint provides on-demand molecular visualisations provided by RDKit.
\begin{table}[H]
\centering
\caption{API endpoint specification.}
\label{tab:endpoints}
\begin{tabular}{llp{6.5cm}}
\hline
\textbf{Method} & \textbf{Path} & \textbf{Description} \\
\hline
\multicolumn{3}{l}{\textit{Prediction}} \\
POST & \texttt{/predict} & Accepts a single drug--target pair and returns a binding probability score. \\
POST & \texttt{/screen} & Accepts up to 100 drugs against a single protein target and returns ranked scores (virtual screening). \\
\hline
\multicolumn{3}{l}{\textit{Autocomplete and Filtering}} \\
GET & \texttt{/autocomplete/drug} & Returns up to 10 drug matches by name or SMILES substring. \\
GET & \texttt{/autocomplete/protein} & Returns up to 10 matching protein target names. \\
GET & \texttt{/autocomplete/drug-sample} & Returns a random sample of drugs for quick screening. \\
GET & \texttt{/drug-filters} & Returns available filter definitions for the frontend to render. \\
GET & \texttt{/drug-filter} & Returns compounds matching physicochemical and activity filters. \\
GET & \texttt{/drug-filter/count} & Returns the count of compounds matching the given filters. \\
\hline
\multicolumn{3}{l}{\textit{Visualisation}} \\
GET & \texttt{/structure/svg} & Returns an Scalable Vector Graphic (SVG) depiction of a molecule from its SMILES string. \\
\hline
\multicolumn{3}{l}{\textit{History}} \\
GET & \texttt{/history} & Returns the most recent 100 predictions and screens. \\
DELETE & \texttt{/history} & Clears all stored prediction history. \\
\hline
\multicolumn{3}{l}{\textit{System}} \\
GET & \texttt{/health} & Returns the health status of the API. \\
\hline
\end{tabular}
\end{table}

\subsection{Data Loading and Preprocessing}
\label{subsec:data_loading}
% CSV ingestion, descriptor caching to pickle, ProtBERT embedding cache,
% binder index construction — all the startup-time work
At startup time, the backend performs several data loading and preprocessing steps to ensure that the system is ready for user interactions.

The full BindingDB CSV containing drug SMILES strings, protein target names, amino acid sequences, and binary interaction labels, is loaded into a pandas DataFrame at module import time by the training data service.
From this, the autocomplete endpoint creates drug and target lists.

Molecular descriptors for all unique compounds are computed using RDKit \citep{rdkit_doc}, and these are persisted to a pickle file so that subsequent startups can load them directly rather than recomputing them.
A staleness check compares the cached descriptor count against the number of unique SMILES in the current dataset, triggering a rebuild if they diverge by more than five per cent.
This was chosen to tolerate small dataset changes to ensure cached data is consistent with the actual dataset.
This is shown in the following code snippet:

\begin{lstlisting}[caption={Descriptor cache loading with staleness check.}, label={lst:cache_load}]
def _load_or_build() -> pd.DataFrame:
    if os.path.exists(_CACHE_PATH):
        cached = pd.read_pickle(_CACHE_PATH)
        n_unique = DRUG_ROWS["drug_smiles"].nunique()
        if abs(len(cached) - n_unique) < n_unique * 0.05:
            return cached
        print("Descriptor cache is stale, rebuilding...")

    table = _build_descriptor_table()
    os.makedirs(_CACHE_DIR, exist_ok=True)
    table.to_pickle(_CACHE_PATH)
    return table
\end{lstlisting}

Protein representations are handled through a hash-indexed cache of precomputed ProtBERT embeddings \citep{Elnaggar2020.07.12.199554}.
Each protein sequence is mapped to a 1024-dimensional embedding vector stored as a NumPy \texttt{.npy} file, keyed by an MD5 hash of the sequence.
At startup the prediction service builds a lookup table to map protein names to their amino acid sequences, reducing retrieval time of embeddings.

To support the known-binders filter, a binder index is constructed by grouping together the training data by target name and collecting the set of SMILES strings that bind.
\subsection{Model Inference Pipeline}
This section describes how raw drug and target inputs become a binding probability score through feature extraction and neural network inference.

\subsubsection{Model Artefact}
The trained model is stored as a \texttt{.pt} file, which is PyTorch's standard serialisation format for model weights.
During training, the training script monitors the validation AUC after each epoch and saves the model's \texttt{state\_dict} --- a Python dictionary mapping each layer name to its learned weight tensor --- whenever a new best validation score is achieved.
This best-checkpoint strategy ensures that the saved file captures the model at its best point rather than just at the end of training where overfitting may have occurred.

The resulting file (\texttt{original\_best.pt}) contains only the learned parameters (weights and biases) of the \texttt{DTI\_DNN} network, not the architecture definition itself.
This means the model class must be available in code at load time so that PyTorch can reconstruct the network and populate it with the saved weights.

\subsubsection{Model Loading at Startup}
When the FastAPI backend starts, the prediction service loads the \texttt{.pt} file once and retains the model in memory for the lifetime of the process.
The loading procedure is as follows:
\begin{enumerate}
    \item An empty \texttt{DTI\_DNN} instance is created with the same architecture hyperparameters used during training: a 167-dimensional drug input passed through a drug encoder (167$\rightarrow$256$\rightarrow$128), a 1024-dimensional protein input passed through a protein encoder (1024$\rightarrow$512$\rightarrow$256$\rightarrow$128), and the resulting 256-dimensional representation passed through the interaction predictor (256$\rightarrow$256$\rightarrow$128$\rightarrow$64$\rightarrow$1), all with a dropout rate of 0.4.
    \item \texttt{torch.load} deserialises the \texttt{.pt} file into a state dictionary, mapping it to the best available hardware accelerator (Apple MPS, CUDA, or CPU).
    \item \texttt{load\_state\_dict} populates the empty model with the saved weights.
    \item The model is set to evaluation mode via \texttt{model.eval()}, which disables dropout and batch normalisation updates so that inference results are deterministic.
\end{enumerate}
Because the model is loaded once at startup rather than on each request, inference latency is kept low and repeated file I/O is avoided.
This is reported in Section~\ref{tab:endpoint_latency} as it measures a single pair prediction at 5ms.

\subsubsection{Feature Extraction and Forward Pass}
The two feature vectors (SMILES, Amino acid sequence) are passed through their respective encoder subnetworks and concatenated into a fused 256-dim representation.
This joint vector is then fed through the interaction predictor head, which outputs a probability score between 0 and 1, where values above 0.3 indicate predicted binding.

\subsection{Virtual Screening Pipeline}
To fully emulate virtual screening, the pipeline also supports batch predictions.
Comapring multiple drug candidates against a single protein, these can be selected through filters or a random sample.
% Filtering by physicochemical properties, MaxMin diversity picking, result ranking
\subsubsection{Filtering}
The virtual screening pipeline allows users to test multiple drugs against a single protein through a series of filters.
Users can apply filters on physiochemical properties such as:
\begin{table}[H]
\centering
\caption{Available compound filters for virtual screening.}
\label{tab:filters}
\begin{tabular}{lp{8cm}}
\hline
\textbf{Filter} & \textbf{Description} \\
\hline
Molecular Weight Range & Molecular weight in Daltons \citep{lipinski2001, veber2002}. \\
Ring Count & Filters by the number of ring structures present in the molecule \citep{bemis1996}. \\
Lipinski's Rule of Five & Enforces MW $\leq$ 500, LogP $\leq$ 5, HBA $\leq$ 10, and HBD $\leq$ 5, selecting compounds with drug-like oral bioavailability~\citep{lipinski2001}. \\
Known Binders & Restricts the pool to compounds with a positive interaction label against a user-selected protein target in the training data. \\
\hline
\end{tabular}
\end{table}
These filters are applied as boolean masks over a precomputed descriptor table to narrow down large compound sets efficiently.
\subsubsection{Limited Selection}
When the number of compounds matching the filters exceeds the processable limit, the system applies the \textit{MaxMin} diversity-picking algorithm using MACCS-based Tanimoto distance~\citep{tanimoto}.
Instead of using a random sample, this algorithm makes sure to select a structurally diverse subset to ensure broad coverage of drugs in the test.
If $>100$ compounds pass the filters, MaxMin selects 100 structurally diverse ones as only $\leq 100$ compounds are scored.
The selected drug molecules are scored against the target protein using the batch inference pipeline, ranked by binding probability, and matched to metadata such as molecular weight, LogP, and ring count before being returned to the frontend.

\subsection{Input Validation and Error Handling}
% Pydantic schemas, Query param constraints, HTTPException patterns
\subsubsection{Input Validation}
Validating the inputs is handled through two complementary mechanisms.
At the schema level, all requests and responses are defined as Pydantic models, which enforce type constraints and use automatic validation of incoming JSON requests~\citep{pydantic}.

For query parameter endpoints, FastAPI's \texttt{Query} annotations are used to enforce value constraints directly in the function signatures.
This allows us to limit the batch size to 100, capping the drug sample endpoint to 50 results, and keeping the Scalable Vector Graphics (SVG) rendering dimensions between 50 and 500 pixels.
FastAPI rejects requests that violate these constraints automatically with 422 unprocessable entity response before reaching the service layer.
\subsubsection{Error Handling}
The service layer will raise \texttt{descriptive exceptions} when errors occur during inference.
These are caught at the route level and returned as HTTP error responses using FastAPI's \texttt{HTTPException}.
Status codes distinguish the differences between client errors (400 for bad input) and server errors (500 for unexpected failures).

\section{Frontend Design}
Creating a simple and intuitive design is essential for usability.
Therefor we utilised a React framework to ensure speed and optimisations for what we need it to achieve.
\subsection{Frontend Framework}
To develop the frontend, we have used Gatsby, a static-site React framework.
It does this by pre-rendering pages into HTML, called Static Site Generation (SSG), resulting in superior performance.
Gatsby benefits from high security with no server-side attacks as it has no server-side database or dependencies, only static files~\citep{gatsby_definitive, gatsby}.

\subsection{User Interface Design}
We designed two workflows so the user can test a single drug to a single protein target, and batch virtual screening to emulate a wet-lab experiment more closely.
\subsubsection{Ethical Considerations Message}
To inform the user of appropriate use, we have a clear message regarding the ethical considerations of the product to prevent misuse and over-reliance on results produced.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/full_page.png}
    \caption{The web page at initial startup with an ethical considerations message}
    \label{fig:full_page}
\end{figure}
\subsubsection{Single comparison}
One input box for the drug input and another for target input, both have placeholder suggestions, and once the user begins typing, a dropdown menu will present the closest match to what's been written in a top-down fashion.
The protein autocomplete has high performance, responding in 1.2\,ms on average, so suggestions appear without delay while typing.
Whereas the drug autocomplete responds in 100\,ms on average.
Once selected and compared, the results appear just below in a simplified form.
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/Autocomplete single pred.png}
        \caption*{Single prediction input with autocomplete dropdown.}
        \label{fig:single_autocomplete}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/single pred results.png}
        \caption*{Single prediction result displaying the binding probability and classification.}
        \label{fig:single_results}
    \end{minipage}
    \caption{Single-comparison workflow: autocomplete input (left) and prediction result (right).}
    \label{fig:single_comparison}
\end{figure}

\subsubsection{Batch Comparison}
To begin the batch comparison, the user can filter for the group of molecules they'd like to compare against a protein.
The user can toggle for Lipinski's rule of five, select a preset for Molecular Weight Range and number of Rings.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/VS filters 1.png}
    \caption{Virtual screening filter controls for physicochemical properties.}
    \label{fig:vs_filters}
\end{figure}
We also allow the user to filter molecules that bind to a specific target to see if there are any crossovers/they're more likely to bind.
At the bottom of the filter section, it will instantly update the number of molecules that match the set of filters applied.
The \texttt{/drug-filter} returns the number of matching compounds within 300\,ms as seen in Table~\ref{tab:endpoint_latency}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/VS filters whole.png}
    \caption{Complete filter panel with known-binders target selection and live compound count.}
    \label{fig:vs_filters_whole}
\end{figure}
Once the user has added up to 100 molecules for testing and selected the target, they can continue with testing.
This will produce a result table sorting the bindings from most likely to least likely and colour code them.
The table also shows meaningful metadata for each molecule, and hovering over the molecule name will produce a widget with the chemical structure of the molecule.
\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/VS whole.png}
        \caption*{Virtual screening overview showing selected compounds and target protein.}
        \label{fig:vs_whole}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/VS results + SVG.png}
        \caption*{Screening results table with colour-coded binding scores and molecular structure popup.}
        \label{fig:vs_results_svg}
    \end{minipage}
    \caption{Batch virtual-screening interface: compound and target selection (left) and ranked results with structure popup (right).}
    \label{fig:batch_comparison}
\end{figure}
\subsection{Autocomplete and Search Functionality}
Autocomplete was a necessary feature to help users quickly identify the drugs and targets in question.
Some have names with over 15 characters, so the autocomplete search makes it more usable for people.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/Autocomplete Long length.png}
    \caption{Autocomplete handling long compound names.}
    \label{fig:autocomplete_long}
\end{figure}
Search is linked to the BindingDB database to have the most up-to-date naming for molecules and targets.
\subsection{Previous Searches}
An important design feature is how the previous searches are visualised.
We have it sorted by date and time occurred, with the ability to view results.
This allows the user to potentially compare results between closely related targets and drug molecules.
We decided to display the previous results as a widget if the user would like to see the information closer.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/Previous searches.png}
    \caption{Prediction history panel showing previous queries sorted by date.}
    \label{fig:history}
\end{figure}
\subsubsection{Comparisons}
Allowing the user to compare previous results is insightful and can highlight certain differences between two near identical searches, valuable when discovering new drug-target combinations.
This provides the user with insightful information into their tests which can hopefully improve the usability of the product.
The comparison can also be between batch screening and single screening, allowing for full customisable testing.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{project screenshots/Comparison.png}
    \caption{Side-by-side comparison of three previous prediction results.}
    \label{fig:comparison}
\end{figure}
\section{Frontend-Backend Integration}
The frontend and backend communicate through HTTP requests to the RESTful API described in Section~\ref{sec:backend_api}.
All the data transmissions use JSON, where the frontend sends user inputs and receives structured responses containing prediction scores, screening results, or autocomplete suggestions.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/single.png}
        \caption{Single-compound prediction flow. The user submits a SMILES string and protein target; the backend computes MACCS and ProtBERT features in parallel, concatenates them, and returns a binding probability from the trained DNN.}
        \label{fig:single-prediction-flow}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{project screenshots/batch.png}
        \caption{Batch virtual screening flow. The frontend polls \texttt{/drug-filter/count} to provide a live candidate count as filters are adjusted, then fetches the candidate set, performs client-side MaxMin diversity selection, and submits the subset to \texttt{/screen} for batch inference. Molecular structures are rendered lazily via \texttt{/structure/svg} on hover.}
        \label{fig:batch-screening-flow}
    \end{subfigure}
    \caption{Frontend–backend interaction flows for the two primary prediction modes.}
    \label{fig:interaction-flows}
\end{figure}


For the autocomplete, endpoints are called on each keystroke with a debounced delay to return up to 10 matches to populate dropdown suggestions.
When predicting a single pair, the frontend issues a POST request to \texttt{/predict} and displays the returned score and binding classification.
For full virtual screening, the frontend initially retrieves the filtered molecules set from \texttt{/drug-filter} endpoint, then submits the selected drugs to \texttt{/screen} for batch scoring.
Molecular structure diagrams are rendered by requesting Scalable Vector Graphics (SVGs) from the \texttt{/structure/svg} endpoint and embedding them directly in the interface as a widget.

Prediction history is fetched from \texttt{/history} on page load and refreshes after each new prediction.

\section{Performance Considerations}
The design choices were made to minimise response times during use, for a better user experience.
Table~\ref{tab:endpoint_latency} reports mean, median, and 95th-percentile wall-clock latencies measured against the running backend on CPU (20 timed requests per endpoint after a single warm-up).

\begin{table}[H]
    \centering
    \begin{tabular}{lrrr}
        \toprule
        Endpoint & Mean (ms) & p50 (ms) & p95 (ms) \\
        \midrule
        \texttt{POST /predict}              & 5.2   & 4.3   & 21.8  \\
        \texttt{POST /screen} ($N=1$)       & 4.3   & 4.4   & 4.6   \\
        \texttt{POST /screen} ($N=100$)     & 64.0  & 64.2  & 64.7  \\
        \texttt{GET\ /autocomplete/protein} & 1.2   & 1.2   & 1.3   \\
        \texttt{GET\ /autocomplete/drug}    & 101.3 & 98.2  & 176.1 \\
        \texttt{GET\ /drug-filter}          & 297.3 & 298.1 & 306.6 \\
        \texttt{GET\ /structure/svg}        & 2.1   & 2.1   & 2.4   \\
        \bottomrule
    \end{tabular}
    \caption{Backend endpoint latencies measured on CPU after warm-up.}
    \label{tab:endpoint_latency}
\end{table}

The most significant improvement is the computations at startup described in Section~\ref{subsec:data_loading}.
By loading the training dataset, computing molecular descriptors, and indexing known binders at server startup, the system avoids expensive computations during inference, which is why a single prediction returns in roughly 5\,ms rather than the hundreds of milliseconds a cold featurisation would require.
ProtBERT embeddings, which are the most costly feature to compute, are precomputed offline and cached as individual \texttt{.npy} files, reducing protein featurisation to a single file read.
Direct measurement confirms this: a cached \texttt{.npy} embedding loads in 0.04\,ms against a 376\,ms ProtBERT forward pass on the same hardware, a roughly $9{,}000\times$ reduction in per-request protein-featurisation cost.

To allow for virtual screening, we run all candidate drugs through the model in a single forward pass using batch inference.
This will exploit CPU or GPU parallelism through PyTorch's tensor operations~\citep{paszke2019pytorch}.
The effect is visible in Table~\ref{tab:batch_scaling}: per-drug latency drops from 4.5\,ms at $N=1$ to 0.64\,ms at $N=100$, a 7$\times$ improvement from parallelism over the batch.

\begin{table}[H]
    \centering
    \begin{tabular}{rrrr}
        \toprule
        Batch size $N$ & Mean (ms) & p95 (ms) & Per-drug (ms) \\
        \midrule
        1   & 4.5  & 4.8  & 4.50 \\
        5   & 7.4  & 8.1  & 1.48 \\
        10  & 10.4 & 11.2 & 1.04 \\
        25  & 18.9 & 20.0 & 0.76 \\
        50  & 35.1 & 38.2 & 0.70 \\
        100 & 64.0 & 64.7 & 0.64 \\
        \bottomrule
    \end{tabular}
    \caption{\texttt{/screen} batch-scaling curve. Per-drug cost decreases sharply with batch size before plateauing, confirming that fixed per-request overhead is amortised across the batch while tensor-level parallelism.}
    \label{tab:batch_scaling}
\end{table}

\section{Security and Robustness}
The frontend is a static web page that is independent of the backend, so no server-side rendering, therefore making server-side attacks redundant.

The backend validates all incoming data through Pydantic schemas and FastAPI query parameter constraints, rejecting improper requests before they reach the service layer.
This provides a degree of protection against injection attacks, as inputs are type-checked and bound-checked before being passed to RDKit or the model.

There is currently no user authentication or account system or sensitive data, heavily reducing the attack surface area.
Therefore, upon deployment the prediction history will be shared among all users.
If the system were to be deployed in a multi-user clinical setting, user isolation through accounts and authentication will need to be added.
\section{Limitations}
The current system has some limitations that must be acknowledged.
There is currently no session management or accounts system, meaning the history section is global instead of for a user.
Acceptable for a prototype, but it should be considered if deployed live.

Another insight that could benefit users would be a structural visualisation of how the bound pair would look at a molecular level.
Currently, users can only view the 2D structure of drug molecules, but there is no binding structure prediction.
Integrating molecular docking tools can address these issues in future work.
